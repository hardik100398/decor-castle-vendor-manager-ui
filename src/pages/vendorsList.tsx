import Layout from "../modules/common/layout"

type Props = {}

export default function VendorsList({ }: Props) {
    return <div className="w-full">
        <h1 className="text-2xl font-bold text-center w-full">Vendors List</h1>
    </div>
}